# Changelog

2026-09-23 · Fase 0 · specs creadas: `requirements.md` v0.4 (136 MUST, 33 SHOULD), `design.md` v0.3 y `tasks.md` v0.3 (86 tareas); un script comprobó que 136/136 MUST y 33/33 SHOULD tienen al menos una tarea
2026-09-23 · Gate · `aprobado fase 0` recibido; `requirements.md` v0.4, `design.md` v0.3 y `tasks.md` v0.3 pasan a estado aprobado
2026-09-23 · T-01 · `pnpm test -- check-vpn.imports` (2/2, incluido un test del detector de imports) y `pnpm lint` (ESLint + Prettier) en verde; `CLAUDE.md` pasa de symlink roto a archivo para que resuelva `AGENTS.md -> CLAUDE.md`; deps de tooling aprobadas por el humano: `typescript-eslint`, `@eslint/js`, `globals`, `eslint-config-prettier`, `@types/node` (TypeScript 6.0, límite del peer de `typescript-eslint`)
2026-09-23 · T-02 · `pnpm test -- check-vpn.exit2` en verde: sin `--target`, sin puerto, sin valor, puerto no numérico, 0 o > 65535 y argumento desconocido imprimen un JSON con `ok: false` y `error.code: INVALID_ARGS` y salen con exit 2
2026-09-23 · T-03 · `pnpm test -- check-vpn.dns check-vpn.tcp check-vpn.latency check-vpn.exit0` en verde (6 tests, 3 corridas seguidas): contra un `net.Server` en `127.0.0.1` con `--target localhost:<puerto>` los tres checks dan `pass`, la latencia sale en `durationMs` del check `latency` y exit 0; `*.invalid` da `dns fail` + `skip`, puerto cerrado da `tcp fail (ECONNREFUSED)`
2026-09-23 · T-04 · `pnpm test -- check-vpn.threshold check-vpn.exit1` en verde (8 tests): con `--latency-threshold-ms 0` el check `latency` da `fail (threshold_exceeded)` y exit 1; con 1000 da `pass`; valores `-1`, `fast` y `1.5` dan `INVALID_ARGS` y exit 2. Nota: los casos exit 1 por puerto cerrado y por DNS ya pasaban desde T-03, que introdujo `ok ? 0 : 1`
2026-09-23 · T-05 · `pnpm test -- check-vpn.timeout-arg check-vpn.check-timeout` en verde (9 tests, suite completa 32/32 en 3 corridas): con DNS o TCP lentos (fixtures precargados con `node --import`, sin red) el check da `fail (timeout)` a los `--timeout-ms` ms (3000 por defecto), el resto queda en `skip`, exit 1, y el proceso sale al vaciar stdout aunque la búsqueda DNS siga pendiente; `0`, `-5` y `soon` dan `INVALID_ARGS`
2026-09-23 · T-06 · `pnpm test -- check-vpn.deadline check-vpn.output-schema` en verde (11 tests; suite completa 43/43 en 10 corridas): con `--deadline-ms 300` el script imprime un JSON con `error.code: DEADLINE_EXCEEDED`, conserva los checks ya terminados y sale con exit 2; `DEFAULT_DEADLINE_MS = 9000` está en el código fuente y es el máximo de `--deadline-ms`; la salida tiene siempre `version`, `ok`, `target`, `checks`, `summary`, `error` y `durationMs`, en una sola línea y sin stderr. Además se corrigió un test inestable de T-05: los timers de Node pueden disparar hasta 1 ms antes
2026-09-23 · T-07 · Manual M-05 OK (tickets B `infra`, C `provisioning`, D `P1` y E `access`: un solo handoff según R-T1, R-T3, R-T5 y R-T2) y M-09 de `triage` OK (Problems sin avisos de tools; `read/readFile`, `edit/createFile` y `edit/editFiles` se ejecutaron). Ejecutadas por Claude Code con `code chat`, detalle en `docs/manual-tests.md`
2026-09-23 · T-08 · Manual M-07 OK: la respuesta y `data/` no contienen la contraseña ni el usuario del ticket F y se recomienda cambiar la contraseña
2026-09-23 · T-09 · Manual M-02 OK: `/triage-ticket` con el ticket A creó `data/tickets/TCK-20260923-000000-dem.json` en `TRIAGED` con `[EMAIL]` y 4 entradas en la bitácora; se corrigió antes el `ticketId` repetido entre ejecuciones (`78f1add`)
2026-09-23 · T-10 · Manual M-01 OK en el 2.º intento: `triage` rechaza `TRIAGED → RESOLVED` y cita `IN_PROGRESS` y `ESCALATED`, con `ticket-lifecycle.instructions.md` cargado por `applyTo`. El 1.er intento encadenó transiciones e inventó un hallazgo; corregido en `78f1add`
2026-09-23 · Decisión D-12 · plan B de R-02 aprobado por el humano: `diagnostics`, `provisioning` y `escalation` pasan a `user-invocable: true` con `disable-model-invocation: true`, porque VS Code 1.138 marcaba `Unknown agent` en los handoffs y en `escalate-ticket` y `run-vpn-diagnostics`; actualizados `CLAUDE.md` §2.3, Anexo B, R-02 y T-11
2026-09-23 · T-11 · Manual M-06 OK: `/escalate-ticket` sobre el ticket D generó el paquete (`operator_request`, `Infraestructura y Redes`) y lo pasó a `ESCALATED` con su bitácora; `escalation` sirve como `agent` de un prompt file tras D-12. M-09 de `escalation` OK
2026-09-23 · T-12 · Manual M-05 (handoff) OK: los botones **Preparar solicitud de aprobación** y **Enviar a aprobación** llevaron el ticket C a `ESCALATED` con `approval_required` y `approvalRequest`. M-09 de `provisioning` OK
2026-09-23 · T-13 · Manual M-05 (handoff lockout) OK: **Diagnosticar** dejó el ticket E en `RESOLVED` con la instrucción `instruct_self_service_unlock`; el `id` de la acción se corrigió en `5d22a27`. M-09 de `diagnostics` OK
2026-09-23 · T-14 · Manual M-03 OK: `/run-vpn-diagnostics` contra `localhost:8443` cargó la skill, ejecutó `check-vpn.js` (exit 0) y dejó el ticket A en `RESOLVED` con un mensaje en español sin jerga; los comandos de terminal ajenos a la skill no se aprobaron y la regla se endureció en `23ababc`
2026-09-23 · T-15 · Manual M-08 OK: con `target: x:1; echo INJECTED` el agente no ejecutó ningún comando y pidió un `host:puerto` válido; el JSONL roto por esa entrada se corrigió en `c4f7787`
2026-09-23 · T-16 · Manual M-04 OK: con `check-vpn.js` renombrado, Node salió con exit 1 sin JSON y el agente registró `skill_resource_unavailable` y enrutó a `escalation`; script restaurado. Ejecución de T-11 a T-16 por Claude Code con `code chat` y clics simulados, detalle en `docs/manual-tests.md`
2026-09-23 · Gate · `aprobado fase 1` recibido: Fase 1 cerrada con 16/16 tareas y las pruebas manuales M-01 a M-09 en verde
2026-09-23 · T-17 · `pnpm -F agent-spec test -- validate.yaml validate.unknown-key` en verde: YAML mal cerrado, clave duplicada y frontmatter que no es un mapa dan `FRONTMATTER_PARSE_ERROR`; `infer`, claves inventadas en agente, handoff, prompt, skill e instrucciones dan `UNKNOWN_FRONTMATTER_KEY` con la ruta de la clave; el `.github/` real carga 10 archivos sin errores
2026-09-23 · T-18 · `pnpm -F agent-spec test -- validate.required-files validate.exit-code validate.output` en verde (7 tests): cada archivo del Anexo D ausente da `REQUIRED_FILE_MISSING` con su ruta (también sin carpeta `.github/`), el CLI sale con 1 si hay errores y con 0 en el `.github/` real, e imprime `CÓDIGO ruta: mensaje` por error. `pnpm spec:validate` corre `packages/agent-spec/src/cli.ts` directamente con Node (type stripping, por eso `engines.node` sube a `>=22.18`)
2026-09-23 · T-19 · `pnpm test -- trace.check` en verde (5 tests: `REQ_WITHOUT_TASK`, `TASK_TEMPLATE_INVALID`, `UNKNOWN_REQ`, `TRACE_TABLE_OUTDATED` y caso OK) y `node scripts/trace.mjs --write && git diff --exit-code specs/tasks.md` sin diferencias: el script reproduce la tabla de la Fase 0 (136/136 MUST, 33/33 SHOULD, 86 tareas). `pnpm spec:validate` encadena `trace.mjs --check` y el validador de `.github/`
2026-09-23 · T-20 · `pnpm -F agent-spec test -- lifecycle.applyTo lifecycle.table lifecycle.status` en verde (12 tests): `applyTo` sin `**/tickets/**` da `LIFECYCLE_APPLYTO_MISSING`; sin encabezado, sin columna `campos obligatorios`, campo fuera de `TicketState` o flag de lista desconocido dan `LIFECYCLE_TABLE_INVALID`; `REOPENED` da `UNKNOWN_STATUS`; el `.github/` real pasa. `tables.ts` lee la primera tabla GFM bajo un encabezado exacto y `lifecycle.ts` compila la tabla con sus predicados (design §3.1)
2026-09-23 · T-21 · `pnpm -F agent-spec test -- skill.frontmatter skill.name skill.description skill.activation` en verde (14 tests): sin `description` o con `name` fuera de `[a-z0-9-]{1,64}` da `SKILL_FRONTMATTER_INVALID`, nombre distinto de la carpeta da `SKILL_NAME_MISMATCH`, 1025 caracteres dan `SKILL_DESCRIPTION_TOO_LONG` y sin «Úsala cuando» da `SKILL_ACTIVATION_MISSING`; un `SKILL.md` que rompe tres reglas recibe los tres códigos; el `.github/` real pasa
2026-09-23 · T-22 · `pnpm -F agent-spec test -- skill.steps skill.resource skill.failure-section skill.timeout` en verde (13 tests): sin 2 pasos numerados da `SKILL_STEPS_MISSING`, un script de `scripts/` sin enlace relativo da `SKILL_RESOURCE_UNLINKED`, sin `## Manejo de fallos` da `SKILL_FAILURE_SECTION_MISSING`, y un `DEFAULT_DEADLINE_MS` igual al timeout o una sección sin la línea `- **Timeout:** <n> s` dan `SKILL_DEADLINE_INVALID`; el `.github/` real pasa (9000 ms < 10 s). El loader lee ahora los scripts de cada skill
2026-09-23 · T-23 · `pnpm -F agent-spec test -- agents.frontmatter agents.handoff-fields agents.visibility` en verde (9 tests): sin `description` o `tools`, o con un tipo erróneo, da `AGENT_FRONTMATTER_INVALID`; un handoff sin `send` o `prompt`, o que no es un mapa, da `HANDOFF_INVALID`; `user-invocable`/`disable-model-invocation` distintos de `policy.ts` o un agente fuera de la política dan `AGENT_VISIBILITY_INVALID`. `policy.ts` recoge el Anexo B con D-12, el registry, el contexto de handoff y las etiquetas prohibidas
2026-09-23 · T-24 · `pnpm -F agent-spec test -- graph.allowed-handoffs agents.tool-permissions agents.unknown-tool` en verde (9 tests): `diagnostics → provisioning` da `HANDOFF_NOT_ALLOWED`, `execute/runInTerminal` en `provisioning` da `TOOL_NOT_PERMITTED` y `web/fetch` da `UNKNOWN_TOOL` (sin duplicarse como `TOOL_NOT_PERMITTED`); el `.github/` real pasa
2026-09-23 · T-25 · `pnpm -F agent-spec test -- graph.acyclic graph.terminal graph.self graph.unknown-target` en verde (12 tests): `diagnostics ↔ triage` da `HANDOFF_CYCLE`, un handoff de `escalation` da `TERMINAL_HAS_HANDOFFS`, un auto-handoff da `SELF_HANDOFF` (sin duplicarse como ciclo) y un destino sin `.agent.md` da `UNKNOWN_HANDOFF_TARGET`; el `.github/` real pasa. `buildHandoffGraph` queda disponible para compilar el grafo del runtime
2026-09-23 · T-26 · `pnpm -F agent-spec test -- agents.handoff-context allowlist.unsafe` en verde (5 tests): un `prompt` de handoff que nombra `redactedText` o `userMessage` da `HANDOFF_CONTEXT_EXCEEDED` (los tokens entre comillas invertidas que no son claves de `TicketState` no cuentan) y las acciones de la allowlist con etiqueta `mfa` o `permissions` dan `UNSAFE_ALLOWLIST_ACTION`; el `.github/` real pasa. Los campos de `TicketState` pasan a `ticket-state.ts`
2026-09-23 · T-27 · `pnpm -F agent-spec test -- routing.triage routing.diagnostics routing.provisioning` en verde (30 tests): `resolveRoute` devuelve destino, regla y motivo para los 19 casos de triage (16 combinaciones categoría × severidad + 3 errores), los 9 de diagnostics y los 2 de provisioning, según R-T1…R-X3 de design §2.1; `ROUTE_INPUT_DOMAINS` enumera los dominios para T-28
2026-09-23 · T-28 · `pnpm -F agent-spec test -- routing.declared routing.deterministic` en verde (5 tests): con `triage` sin handoff a `provisioning`, la regla R-T3 da `ROUTE_WITHOUT_HANDOFF`; una R-T1 sin la guarda de severidad (2 reglas para infra/P1) y la falta de R-T5 (0 reglas para P1) dan `ROUTING_NOT_DETERMINISTIC`; `routing.ts` y el `.github/` real pasan
2026-09-23 · T-29 · `pnpm -F agent-spec test -- prompts.frontmatter prompts.agent prompts.variables prompts.tool-call` en verde (13 tests): sin `agent` o `tools` da `PROMPT_FRONTMATTER_INVALID`, un `agent` sin `.agent.md` da `UNKNOWN_PROMPT_AGENT`, las variables obligatorias ausentes de REQ-2.4-03..05 dan `PROMPT_VARIABLE_MISSING` (se aceptan con placeholder), un cuerpo sin `#tool:` da `PROMPT_WITHOUT_TOOL_CALL` y una referencia fuera de `tools` da `PROMPT_TOOL_UNDECLARED`; el `.github/` real pasa
2026-09-23 · T-30 · `pnpm spec:validate` sale con 0 sobre el `.github/` real y `pnpm -F agent-spec test -- validate.golden` en verde (2 tests): cero errores y el compilado esperado (4 agentes, 5 handoffs, 12 transiciones, 2 acciones de allowlist, timeout de 10 s y las variables de los 3 prompts). Pasó al primer intento porque cada tarea anterior mantuvo el `.github/` real en verde; una mutación temporal (`infer: true` en `triage.agent.md`) hizo fallar el test y el CLI, y se revirtió
2026-09-23 · T-31 · `pnpm -F api build` en verde y `pnpm -F api test -- llm.test-mode` en verde (3 tests): con `NODE_ENV=test` el selector elige el `FakeChatModel` aunque existan variables de Azure y Anthropic, el módulo Nest lo inyecta y responde igual a la misma entrada. Riesgo R-04 resuelto: NestJS 12 es ESM nativo, compila con `tsc` y `dist/main.js` arranca (HTTP 404 en `/`)
2026-09-23 · T-32 · `pnpm -F api test -- bootstrap.invalid-spec` en verde (2 tests): con un `.github/` inválido el arranque de Nest falla con `SpecValidationError` y la lista `CÓDIGO ruta: mensaje`, y con el real expone el bundle validado. Comprobado también con el binario: `dist/main.js` arranca con la spec real y sale con 1 listando el error cuando se añade `infer: true` (revertido). `.env.example` creado según design §12.5
2026-09-23 · T-33 · `pnpm -F api test -- llm.provider` en verde (5 tests): sin variables de LLM, con Azure incompleto o con la clave de Anthropic vacía, el selector usa el `FakeChatModel` con el aviso `NO_PROVIDER_WARNING`, el módulo Nest lo registra con `Logger.warn` y en modo test no avisa. `dist/main.js` arranca sin `NODE_ENV` mostrando el aviso
2026-09-23 · T-34 · `pnpm -F api test -- redact.email redact.phone redact.token redact.secret` en verde (26 tests; 33 con `redact.identity`): emails → `[EMAIL]`, teléfonos → `[PHONE]` (sin tocar el `ticketId`, IPs ni puertos), JWT, `Bearer`, prefijos conocidos y cadenas largas → `[TOKEN]`, credenciales → `<palabra>: [SECRET]`, documentos → `[ID]` y usuarios → `<palabra>: [USER]`, con conteos por tipo. Desvíos DC-35 y DC-36 registrados
2026-09-23 · T-35 · `pnpm -F api test -- redact.userref` en verde (8 tests): el redact node guarda en `entities.userRef` el valor `usr_` + 8 hex del HMAC-SHA256 del primer email (o, sin él, del primer usuario), normalizado a minúsculas; sin identificador da `usr_unknown`; es estable con la misma sal y cambia con otra; la salida no contiene el identificador original. Sin `REDACTION_SALT` se genera una sal aleatoria con el aviso `MISSING_SALT_WARNING`, que se registrará al conectar el nodo al grafo
2026-09-23 · T-36 · `pnpm -F api test -- audit.append-only audit.jsonl audit.redaction` en verde (8 tests): `AuditLog` solo expone `append` y `read`, añade una línea JSON por entrada en `data/audit/<ticketId>.jsonl` con `ts` y `seq` sin tocar las anteriores, devuelve las entradas en orden de escritura, rechaza un `ticketId` inválido antes de tocar el disco y redacta cada texto (email, secreto, token, teléfono) antes de escribir
2026-09-23 · T-37 · `pnpm -F api test -- lifecycle.compiled lifecycle.invalid lifecycle.closed` en verde (11 tests): una tabla de fixture con una fila extra (`RESOLVED → IN_PROGRESS`) hace que la máquina acepte esa transición y la tabla real la rechaza; al arrancar, `TicketsModule` compila las 12 transiciones reales; de los 49 pares de estados solo se aceptan esos 12, y el resto (incluido todo lo que sale de `CLOSED`) da `INVALID_TRANSITION` citando las transiciones permitidas
2026-09-23 · T-38 · `pnpm -F api test -- lifecycle.required-fields lifecycle.resolved` en verde (12 tests): `validateTransition` comprueba primero la fila y después los predicados de `campos obligatorios` sobre el ticket candidato; faltas en T1, T3, T6, T7 (sin `userMessage`, REQ-COM-01) y T8 (`issueType` en `unknown`) dan `MISSING_REQUIRED_FIELDS`, y a `RESOLVED` sin hallazgo concluyente, sin acción de la allowlist o sin mensaje da `RESOLUTION_INCOMPLETE`, con la lista de lo que falta. `TicketState` y el resto de contratos de design §4 quedan en `tickets/ticket-state.ts`
2026-09-23 · T-39 · `pnpm -F api test -- audit.transition` en verde (4 tests): `TicketLifecycle.applyTransition` escribe en la bitácora `ts`, `agent`, `decision: transition`, `reason`, `from` y `to` y guarda el nuevo estado en el mismo paso; una transición inválida deja `transition_rejected` con su código y no cambia el ticket guardado. `TicketStore` escribe `data/tickets/<ticketId>.json` de forma atómica (archivo temporal + `rename`), sin `audit`, y valida el `ticketId`
2026-09-23 · T-40 · `pnpm -F api test -- allowlist.compiled allowlist.reject lifecycle.escalated-no-remediation` en verde (5 tests): una allowlist de fixture con una acción extra hace que el servicio la acepte y la real la rechaza; al arrancar, `ToolsModule` carga `instruct_vpn_reconnect` e `instruct_self_service_unlock` de `diagnostics.agent.md`; una acción fuera de la allowlist, o cualquier acción sobre un ticket `ESCALATED`, da `ACTION_NOT_ALLOWLISTED` y deja `action_rejected`; una acción válida devuelve `ExecutedAction` con `allowlisted: true` y deja `action_executed`
2026-09-23 · T-41 · `pnpm -F api test -- graph.compile graph.system-prompt` en verde (6 tests): al arrancar, `GraphModule` compila un grafo LangGraph con el nodo `redact` y un nodo por `.agent.md` con su cuerpo como system prompt (metadatos del nodo), un edge por cada uno de los 5 handoffs declarados, las entradas `redact → triage/diagnostics/escalation` y los finales de `diagnostics` y `escalation`; las tools se resuelven contra un registry igual a `REGISTRY_TOOLS` y una tool desconocida impide compilar. Los nodos aún no cambian el estado (DC-43)
2026-09-23 · T-42 · `pnpm -F api test -- triage.category triage.entities triage.severity-matrix` en verde (25 tests): el nodo triage recibe el texto del redact node (sin el email, con `triage.agent.md` como system prompt), asigna una sola `category`, extrae `service`, `issueType`, `businessImpact` y la solicitud de acceso, y deriva `severity` de la matriz compilada de `triage.agent.md` (una tabla modificada cambia el resultado; «nadie… sede… detenida» da P1). Salida estructurada con `withStructuredOutput(zod)` sobre el `FakeChatModel` por palabras clave; bitácora `redacted` (solo conteos) y `classified`. El grafo del runtime ya usa ambos nodos
2026-09-23 · T-43 · `pnpm -F api test -- audit.routing` en verde (6 tests): `withRouting` aplica `routing.ts` al terminar el nodo triage, escribe en la bitácora una entrada `routed` con el destino y la regla (R-T1…R-T5, con motivo cuando lo hay) y guarda `lastRoute` y `nextAgent`; el grafo en ejecución va de triage al destino de la decisión (bitácora `redacted`, `classified`, `routed`). Cada nodo solo sigue su propia decisión de enrutamiento
2026-09-23 · T-44 · `pnpm -F api test -- escalation.package escalation.status escalation.terminal` en verde (4 tests): el nodo escalation arma el paquete con `ticketId`, `category`, `severity`, `entities`, `findings` y el motivo de la decisión de enrutamiento (`critical_severity`, `unknown_category`), pone `userMessage` con la plantilla `escalated`, pasa el ticket a `ESCALATED` (guardado en el store, con bitácora `escalated` y `transition`) y el grafo termina ahí. Triage aplica ahora `NEW → TRIAGED`; las plantillas se compilan de `copilot-instructions.md`
2026-09-23 · T-45 · `pnpm -F api test -- escalation.llm-down escalation.fallback escalation.user-message` en verde (5 tests): con el LLM caído al redactar, el paquete sale de la plantilla determinista con `message_replaced` (`llm_unavailable`); con un error inesperado del nodo (`TypeError`) se registra `error` y el ticket igual queda `ESCALATED` con la plantilla, mientras un error de E/S (`EACCES`) detiene el recorrido; `userMessage` siempre lleva el `ticketId` y un borrador sin él se reemplaza por la plantilla
2026-09-23 · T-46 · `pnpm -F api test -- escalation.approval-request` en verde (3 tests): para un ticket `provisioning`, el paquete de escalamiento lleva `approvalRequest` derivado sin LLM de `entities.request` y `entities.userRef` (`resource`, `accessLevel`, `requesterRef`, `justification`, `complete` y el `summary` de la plantilla `internal.approval`); sin justificación queda `complete: false`, y otras categorías no lo llevan
2026-09-23 · T-47 · `pnpm -F api test -- llm.failure llm.invalid-output` en verde (5 tests): si la clasificación de triage lanza un error o supera el timeout (`LLM_TIMEOUT_MS`, 30 s por defecto), el grafo enruta a escalation por R-X1 con `llm_unavailable`; si la salida no cumple el esquema zod, por R-X2 con `invalid_llm_output`. En ambos casos queda la entrada de bitácora del fallo y el ticket pasa de `NEW` a `ESCALATED`
2026-09-23 · T-48 · `pnpm -F api test -- graph.handoff-context` en verde (3 tests): el LLM de escalation recibe solo el system prompt y un `HandoffEnvelope` con `ticketId`, `category`, `severity`, `entities` y `findings`, más la decisión de enrutamiento y el `prompt` del handoff declarado en el `.agent.md` de origen (triage o provisioning); ningún mensaje contiene `redactedText` ni `approvalRequest`
2026-09-23 · T-49 · `pnpm -F api test -- vpn-tool.run vpn-tool.timeout audit.tool` en verde (5 tests): `check-vpn.js` se compila desde el enlace del `SKILL.md` con su timeout de 10 s y corre como proceso hijo sin shell; un script de fixture que bloquea el event loop se mata con `SIGKILL` al vencer el timeout (`exitCode: null`); cada ejecución deja una entrada `tool_run` con la tool, el estado (`ok`/`timeout`) y la duración
2026-09-23 · T-50 · `pnpm -F api test -- vpn-tool.failure vpn-tool.finding` en verde (8 tests): con timeout, exit `2`, salida no JSON o script ausente (exit `1` sin JSON), el tool da el *outcome* `resource_unavailable` (R-D5 → escalation con `skill_resource_unavailable`), un hallazgo no concluyente y una entrada `skill_resource_unavailable` con el exit code y la duración; con exit `0` da un hallazgo `gateway_healthy`, y con exit `1` la causa del primer check fallido (`dns_failure`, `gateway_unreachable`, `high_latency`)
2026-09-23 · T-51 · `pnpm -F api test -- graph.vpn-route graph.vpn-ok graph.vpn-unhealthy` en verde (4 tests): un ticket `infra`/`vpn` llega a diagnostics y ejecuta la skill `vpn-diagnostics` (`tool_run`); con exit `0` se entrega `instruct_vpn_reconnect`, `userMessage` sale de su plantilla y el ticket pasa `TRIAGED → IN_PROGRESS → RESOLVED` (R-D1); con exit `1` queda el hallazgo `gateway_unreachable` y escala con `vpn_gateway_unhealthy` (R-D4, `IN_PROGRESS → ESCALATED`); un ticket de identidad (`mfa`) no ejecuta la skill y escala con `requires_identity_action`
2026-09-23 · T-52 · `pnpm -F api test -- graph.access-lockout` en verde (1 test): un ticket `access`/`lockout` no ejecuta la skill, deja un hallazgo de regla `account_locked_by_retries`, entrega `instruct_self_service_unlock` (`action_executed`), pone el `userMessage` de su plantilla y termina `RESOLVED` por R-D2
2026-09-23 · T-53 · `pnpm -F api test -- graph.waiting-user` en verde (2 tests): un ticket `infra` o `access` con `issueType: unknown` pasa `TRIAGED → IN_PROGRESS → WAITING_USER`, con el `userMessage` de la plantilla `waiting_user` que pide el dato que falta, y termina el recorrido por R-D3
2026-09-23 · T-54 · `pnpm -F api test -- allowlist.escalate` en verde (1 test): con la fila `instruct_self_service_unlock` quitada de la allowlist de `diagnostics.agent.md`, el servicio de acciones rechaza la acción (`action_rejected` con `ACTION_NOT_ALLOWLISTED`), no se añade ninguna acción y el ticket escala por R-D8 con `action_not_allowlisted` (`IN_PROGRESS → ESCALATED`)
2026-09-23 · T-55 · `pnpm -F api test -- provisioning.request` en verde (4 tests): provisioning normaliza `entities.request` a exactamente `resource`, `accessLevel` y `justification` (espacios recortados, `license` para licencias, `read` y `''` por defecto), la guarda en el ticket con `TRIAGED → IN_PROGRESS`, registra `node_finished` con el recurso y el nivel, y envía a aprobación por R-P1 (`approval_required`)
2026-09-23 · T-56 · `pnpm -F api test -- graph.internal-error` en verde (2 tests): sin la plantilla `resolved.instruct_self_service_unlock`, el nodo diagnostics falla después de tomar el caso; el envoltorio registra `error`, enruta por R-X3 con `internal_error` y escalation cierra con `IN_PROGRESS → ESCALATED`; un `TypeError` en triage también se convierte en la ruta R-X3
2026-09-23 · T-57 · `pnpm -F api test -- audit.write-failure store.write-failure` en verde (2 tests): con la carpeta `audit/` sin permiso de escritura, el recorrido se detiene con `AUDIT_WRITE_FAILED` y no se guarda el ticket; con `tickets/` sin permiso, se detiene con `STORE_WRITE_FAILED` después de la entrada `transition`, y queda una entrada `error` con ese código
2026-09-23 · T-58 · `pnpm -F api test -- guard.credential-request guard.jargon guard.internal-data` en verde (19 tests): la guarda salta con peticiones de contraseña, clave, token o código MFA (y no con «indica el código del caso» ni «indica el paso clave»), con términos de la lista de jerga como palabra completa, con un `userRef` o un fragmento JSON, y con un mensaje escalado sin `ticketId`; un borrador de escalation que rompe una regla se reemplaza por la plantilla `escalated` con `message_replaced` y la regla en `data.cause`
2026-09-23 · T-59 · `pnpm -F api test -- llm.drafting-fallback` en verde (5 tests): si el borrador de escalation supera el timeout se usan las plantillas `internal.summary` y `escalated`; si el borrador del `userMessage` de diagnostics falla o supera el timeout, queda la plantilla de la acción con `message_replaced` (`llm_unavailable`) y el ticket igual termina `RESOLVED`; un borrador válido se conserva, y uno que pide la clave se reemplaza (`credential_request`)
2026-09-23 · T-60 · `pnpm -F api test -- redact.persistence redact.logs` en verde (2 tests): cuatro tickets con PII sintética (correo, teléfono, JWT, contraseña, cédula y usuario) recorren las ramas VPN, bloqueo, provisioning e identidad; ningún archivo de `data/` (tickets y bitácoras) ni la salida de `console`, stdout o stderr contiene alguno de los valores originales. Los tests pasaron sin cambios en el runtime; una aserción comprueba que los seis valores sí están en la entrada
2026-09-23 · T-61 · `pnpm -F api test -- prompts.render prompts.entry-agent redact.prompt-variables` en verde (8 tests): `POST /prompts/:name/run` responde `202 { ticketId }` y registra `prompt_run`; `triage-ticket` arranca en triage con el template renderizado como mensaje del LLM, `run-vpn-diagnostics` en diagnostics sobre el `target` pedido y `escalate-ticket` en escalation con `operator_request`; las variables `ticket` y `reason` llegan redactadas al LLM (`[EMAIL]`, `[PHONE]`) y no aparecen en `data/`. Prueba manual: el api compilado respondió `202` y el ticket terminó `RESOLVED`
2026-09-23 · T-62 · `pnpm -F api test -- prompts.missing-variable prompts.not-found prompts.unknown-ticket` en verde (8 tests): una variable ausente o vacía responde `400` con sus nombres en `missing` (y una variable que no es texto también da `400`); un prompt inexistente responde `404`; un `ticketId` inexistente o malformado (`../../etc/passwd`) responde `404`; en ningún caso se escribe en `data/`
2026-09-23 · T-63 · `pnpm -F api test -- prompts.target-format prompts.target-allowlist` en verde (8 tests): un `target` que no cumple `^[a-z0-9.-]+:\d{1,5}$` (`x:1; echo INJECTED`, mayúsculas, sin puerto, con ruta o con más de 5 dígitos) o fuera de `VPN_ALLOWED_TARGETS` responde `400` sin tocar `data/`; un `target` de la lista responde `202`; sin la variable, la lista es la de `.env.example`
2026-09-23 · T-64 · `pnpm -F api test -- lifecycle.sla` en verde (4 tests): la tabla `## SLA` de `ticket-lifecycle.instructions.md` se compila a horas por severidad; al clasificar, triage pone `slaDueAt = createdAt + objetivo` (P1 4 h, P3 24 h, P4 72 h en los casos probados) y el ticket guardado lo conserva; una severidad sin fila da error
2026-09-23 · T-65 · `pnpm -F api test -- tickets.create` en verde (1 test): `POST /tickets { text, channel }` responde `202` con un `ticketId` nuevo; la bitácora empieza con `prompt_run` (`triage-ticket`), `redacted` y `classified`, y el ticket llega a `RESOLVED` con su canal
2026-09-23 · T-66 · `pnpm -F api test -- tickets.list tickets.detail tickets.audit` en verde (7 tests): `GET /tickets` lista los tickets con `ticketId`, `category`, `severity`, `status`, `slaDueAt` y `createdAt`, del más reciente al más antiguo (vacía si no hay); `GET /tickets/:id` devuelve el `TicketState` sin `audit`; `GET /tickets/:id/audit` devuelve las entradas en orden de escritura (`seq` 1, 2, 3); un id inexistente o malformado responde `404`
2026-09-23 · T-67 · `pnpm -F api test -- tickets.close` en verde (4 tests): un ticket `RESOLVED` o `ESCALATED` pasa a `CLOSED` con su `closeReason`, guardado y con la entrada `transition` del operador; desde `IN_PROGRESS` responde `409` con `INVALID_TRANSITION` y no cambia el estado; un `closeReason` fuera de la lista da `400` y un ticket inexistente `404`
2026-09-23 · T-68 · `pnpm -F api test -- tickets.reply` en verde (5 tests): con un ticket `access` en `WAITING_USER`, `POST /tickets/:id/reply { issueType: lockout }` responde `202`, registra `WAITING_USER → IN_PROGRESS` del runtime, reanuda el grafo en diagnostics (entrega `instruct_self_service_unlock`) y el ticket termina `RESOLVED` con el nuevo `issueType`; un ticket fuera de `WAITING_USER` da `409 INVALID_TRANSITION` y un `issueType` ausente, `unknown` o desconocido da `400` sin cambiar el estado
2026-09-23 · T-69 · `pnpm -F api test -- health` en verde (2 tests; el patrón también encuentra 1 de `llm.provider`): `GET /health` responde `{ llmProvider: 'fake', spec: { valid: true, errors: [] } }` con la spec del repositorio, y la validación de una carpeta `.github/` inválida devuelve `valid: false` con sus errores
2026-09-23 · T-70 · `pnpm -F api test -- validation.body` en verde (5 tests): un body que no cumple su esquema zod en `POST /prompts/:name/run`, `POST /tickets`, `POST /tickets/:id/reply` y `POST /tickets/:id/close` responde `400` con `errors`, cada uno con la ruta del campo y el mensaje (`variables.ticket`, `channel`, `text`, `issueType`, `closeReason`)
2026-09-23 · T-71 · `pnpm -F api test -- escalation.target-team` en verde (5 tests): el paquete de escalamiento lleva `targetTeam` según la categoría, con la tabla de `escalation.agent.md` (`unknown` → Mesa de Ayuda N2, `access` → Identidad y Accesos, `infra` → Infraestructura y Redes, `provisioning` → Gestión de Accesos (aprobadores)); una categoría sin fila no lleva equipo
2026-09-23 · T-72 · `pnpm -F agent-spec test -- pii.fixtures` en verde (5 tests): `pnpm spec:validate` informa `PII_IN_FIXTURE` (con la línea, sin repetir la dirección) cuando `.github/` o un fixture de pruebas tiene un correo fuera de `example.com` y `example.internal`, ignora `node_modules` y termina con código 1; el repositorio actual no tiene ninguno
2026-09-23 · T-74 · `pnpm -F web build` y `pnpm -F web test -- app.smoke` en verde (1 test): con Angular 22.2 (standalone, zoneless, `ng test` con Vitest en jsdom), abrir `/` redirige a `/tickets` y muestra la bandeja; con el redirect cambiado el test falla. `pnpm test` en la raíz ejecuta los tests de web con un test puente; `pnpm lint` y `pnpm spec:validate` en verde. Decisiones DW-01..DW-05
2026-09-24 · T-75 · `pnpm -F web test -- inbox` en verde (3 tests): la bandeja pide `GET /api/tickets` con `ApiService` y lista `ticketId`, categoría, severidad, estado y plazo de atención (`slaDueAt`), con «Pendiente» en los campos aún sin clasificar, un mensaje mientras carga y un aviso si la petición falla; quitar la columna de severidad hace fallar el test. El proxy del dev server reescribe `/api/tickets` → `/tickets` en el puerto 3000 (comprobado con un servidor de prueba). Verificado contra el contrato con HTTP simulado; integración real pendiente de T-66/T-73. Decisiones DW-06..DW-08
2026-09-24 · T-76 · `pnpm -F web test -- timeline` en verde (3 tests): al abrir `/tickets/:id` (por URL o desde el enlace de la bandeja) la web pide `GET /api/tickets/:id` y `GET /api/tickets/:id/audit` y muestra el recorrido derivado de la bitácora: pasos visitados con su duración (de `node_started` a `node_finished`, o «En curso»), derivaciones (`routed`), cambios de estado (`transition`) y comprobaciones (`tool_run`) con su motivo; un ticket inexistente (404) muestra un aviso. Ignorar las entradas `routed` hace fallar el test. Verificado contra el contrato con HTTP simulado; integración real pendiente de T-66/T-73. Decisiones DW-09 y DW-10
2026-09-24 · T-77 · `pnpm -F web test -- audit-view` en verde (2 tests): en el detalle, la pestaña «Bitácora» muestra las entradas de `GET /api/tickets/:id/audit` en el orden en que el api las escribió (`seq` 1…13 y el código de cada decisión coinciden con la respuesta), con responsable, decisión (etiqueta en español y código), motivo, estados origen → destino y los datos de la entrada; invertir el orden en la vista hace fallar ambos tests. Verificado contra el contrato con HTTP simulado; integración real pendiente de T-66/T-73
2026-09-24 · T-78 · `pnpm -F web test -- redacted-only` en verde (1 test): el detalle muestra la descripción del problema solo desde `redactedText` (con marcadores como `[EMAIL]`); aunque la respuesta traiga un campo `rawText` ajeno al contrato con un email, un teléfono y una clave sintéticos, ninguno de esos valores aparece en la página. Mostrar `rawText` en la plantilla hace fallar el test. Verificado contra el contrato con HTTP simulado; integración real pendiente de T-66/T-73
2026-09-24 · T-79 · `pnpm -F web test -- timeline.sse` en verde (4 tests): al abrir el detalle, `EventsService` abre `EventSource` en `/api/tickets/:id/events`; las entradas `audit` que llegan se añaden al recorrido y a la bitácora sin recargar la página ni repetir el GET, y las que ya trajo el GET (mismo `seq`) no se duplican; con `done` se recarga el ticket (el estado pasa a «Resuelto») y se cierra la conexión. Sin la fusión en vivo fallan 3 tests y sin quitar duplicados falla 1. Verificado contra el contrato con HTTP y SSE simulados (`EventSource` falso); integración real pendiente de T-66/T-73. Decisión DW-11
2026-09-24 · T-80 · `pnpm -F web test -- timeline.reconnect` en verde (3 tests): si la conexión SSE cae, `EventsService` la cierra y abre una nueva a los 5 s (a los 4999 ms sigue sin reconectar), y repite cada 5 s mientras siga cayendo; los eventos de la conexión nueva llegan al recorrido sin duplicar lo ya mostrado; tras `done` o al salir del detalle deja de reintentar. Con un retraso de 3 s falla 1 test y sin reintento fallan 2. Verificado contra el contrato con SSE simulado (`EventSource` falso y temporizadores simulados); integración real pendiente de T-73
2026-09-24 · T-81 · `pnpm -F web test -- new-ticket` en verde (3 tests): el formulario `/tickets/new` envía `POST /api/prompts/triage-ticket/run` con `{ variables: { ticket, channel } }` (texto sin espacios sobrantes y el canal elegido) y, con el `202 { ticketId }`, abre el detalle del ticket nuevo para seguir su recorrido; una descripción vacía no se envía y muestra «Describe el problema.»; si el api falla, avisa y conserva el texto. Con otro prompt fallan los 3 tests y sin la validación falla 1. Verificado contra el contrato con HTTP simulado; integración real pendiente de T-61/T-66/T-73. Decisiones DW-13 y DW-14
2026-09-23 · Gate · `aprobado fase 2` recibido (como «Fase 2 aprobada», DC-73): Fase 2 cerrada con T-17..T-72; DA-01 (DeepSeek y LM Studio), DA-02 (fijar Node) y DA-03 (eventos de nodo) decididos; T-87 y T-88 añadidas a la Fase 3
2026-09-23 · T-73 · `pnpm -F api test -- tickets.sse` en verde (4 tests): `GET /tickets/:id/events` responde `text/event-stream` con cada entrada de la bitácora como evento `audit` (id = `seq`, incluidas `routed` y `transition`), primero las ya escritas y luego las nuevas, sin repetir `seq`, y termina con `done { status }` al acabar el recorrido; un ticket nuevo transmite mientras triage espera al LLM, aunque aún no esté guardado; sin recorrido en marcha reenvía la bitácora y manda `done` enseguida; un id desconocido o mal formado da 404. Suite completa 453/453, lint y `spec:validate` en verde (Node 22.23.3). Decisiones DC-74 y DC-75; DA-04 abierta
2026-09-23 · T-88 · `pnpm test -- node.version` en verde (4 tests): `.nvmrc` fija 22.23.3, el `engines` de la raíz es igual al de Angular CLI (`^22.22.3 || ^24.15.0 || >=26.0.0`) y 22.23.3 cae dentro; sin `.nvmrc` o con el `engines` anterior el test falla. `pnpm install` con Node 22.19 sale con exit 1 y `ERR_PNPM_UNSUPPORTED_ENGINE` (gracias a `engine-strict` en `.npmrc`); con 22.23.3 instala. Decisiones DC-76 y DC-77
2026-09-23 · T-87 · `pnpm -F api test -- graph.node-events` en verde (4 tests): cada nodo visitado (redact, triage, diagnostics o provisioning, escalation) escribe `node_started` antes de sus demás entradas y `node_finished` con `durationMs` al final, con su `routed` en medio; si el nodo falla con `StoreWriteError`, `node_finished` lleva `data.error` y el error sigue; con `AuditWriteError` no se reintenta. Sin el envoltorio fallan los 4. Suite completa 461/461, lint y `spec:validate` en verde (Node 22.23.3). Decisiones DC-78 y DC-79
2026-09-23 · Gate · `aprobado fase 3` recibido tras el E2E con LM Studio (`docs/manual-tests.md`): 7/7 tickets recorren el grafo con SSE igual a la bitácora y sin PII en `data/`; DA-04 confirmado (404 en el detalle durante la clasificación)
2026-09-23 · Spec · Fase 4 replanteada con tus respuestas: proveedores Azure OpenAI → DeepSeek → LM Studio → fake (REQ-LLM-02 reescrito, REQ-LLM-05 nuevo, ADR-04, design §12.3/§12.5, CLAUDE.md §3.5), T-83 reescrita, T-89 (LM Studio) y T-90 (ticket en `NEW`, REQ-API-12) nuevas; `@vitest/coverage-v8` aprobada para T-85
2026-09-23 · T-82 · `pnpm -F api test -- llm.provider` en verde (8 tests): con `AZURE_OPENAI_ENDPOINT`, `AZURE_OPENAI_API_KEY` y `AZURE_OPENAI_DEPLOYMENT` sintéticos (sin red), el runtime elige `AzureChatOpenAI` con ese endpoint y deployment, la versión de API de `AZURE_OPENAI_API_VERSION` o `2024-10-21`, `temperature` 0 y `maxTokens` 1024, sin aviso; el módulo Nest lo informa como `azure`; con la configuración incompleta sigue el modelo fake con aviso. Sin la rama de Azure fallan los 3 tests nuevos. Suite completa 464/464, lint y `spec:validate` en verde. Dependencia `@langchain/openai` 1.5.13 (permitida). Decisiones DC-81 y DC-82
2026-09-23 · T-83 · `pnpm -F api test -- llm.provider` en verde (12 tests): sin Azure y con `DEEPSEEK_API_KEY` sintética, el runtime elige DeepSeek con `ChatOpenAI` (`deepseek-chat` o `DEEPSEEK_MODEL`, `temperature` 0, `maxTokens` 1024); Azure tiene prioridad si están ambos; con `fetch` simulado (sin red), la salida estructurada va a `https://api.deepseek.com/chat/completions` como function call (`tools` con `classify_ticket`, sin `response_format`) y se devuelve parseada. Sin forzar function calling falla ese test. `.env.example` cambia Anthropic por DeepSeek. Suite completa 468/468, lint y `spec:validate` en verde. Decisión DC-83
2026-09-23 · T-84 · `pnpm test -- ci.pipeline` en verde (7 tests): `azure-pipelines.yml` se dispara en todas las ramas sobre `ubuntu-latest`, toma Node de `.nvmrc` (`NodeTool@0`), hace `corepack enable` y `pnpm install --frozen-lockfile`, y después, sin `continueOnError`, `pnpm spec:validate`, `pnpm lint`, `pnpm test` (JUnit) y la cobertura de agent-spec, antes de `pnpm -r build`; publica JUnit y Cobertura con `succeededOrFailed()`. Sin el archivo falla el test. **Pendiente:** la ejecución en Azure DevOps, que requiere conectar el repo a un proyecto de Azure DevOps. Decisión DC-84
2026-09-23 · T-85 · `pnpm -F agent-spec test -- --coverage` en verde (143 tests, 95,4 % de líneas) con umbral del 90 % en su `vitest.config.ts`; con el umbral al 100 % el comando sale con exit 1 («Coverage for lines … does not meet global threshold (100%)»). Dependencia `@vitest/coverage-v8` aprobada. Decisión DC-85
2026-09-23 · T-86 · `pnpm -F agent-spec test -- docs.readme-graph` en verde (3 tests): `pnpm spec:graph` imprime el flowchart Mermaid del grafo compilado desde `.github/` y es idéntico al bloque incrustado en `README.md`; sin el README o sin las funciones exportadas fallan los 3. README en español con arquitectura, diagrama, requisitos, comandos, proveedores LLM por variables, guion de demo, SDD y CI. Suite completa 478/478, lint y `spec:validate` en verde. Decisión DC-86
2026-09-23 · T-90 · `pnpm -F api test -- tickets.created-early` en verde (2 tests): con triage retenido en su llamada al LLM, `GET /tickets/:id` ya responde 200 con `status: NEW`, el canal y solo `redactedText` (`[EMAIL]`); el archivo del ticket no contiene el correo, y la bitácora es `prompt_run → node_started → redacted → ticket_created → node_finished → triage:node_started`; una corrida sobre un ticket guardado no escribe `ticket_created`. Sin el cambio falla el primero (404). Si el disco falla, el redact node registra `error` con `STORE_WRITE_FAILED` y el recorrido se detiene (`store.write-failure` ajustado). Suite completa 484/484, lint y `spec:validate` en verde. Decisión DC-87
2026-09-23 · T-89 · `pnpm -F api test -- llm.provider` en verde (16 tests): sin Azure ni DeepSeek y con `LMSTUDIO_BASE_URL` y `LMSTUDIO_MODEL` sintéticos, el runtime elige LM Studio con `ChatOpenAI` (`temperature` 0, `maxTokens` 1024); DeepSeek tiene prioridad; con `fetch` simulado, la salida estructurada va a `<LMSTUDIO_BASE_URL>/chat/completions` con `response_format` `json_schema` y `reasoning_effort: none`; una URL sin modelo sigue en el fake con aviso. E2E real con LM Studio (`qwen/qwen3.5-9b`) registrado en `docs/manual-tests.md`: `/health` informa `lmstudio`, 7/7 recorridos con SSE igual a la bitácora, detalle en 200 a los 100 ms (T-90), sin PII en `data/`; 6/7 borradores cayeron a la plantilla por la calidad del modelo. Suite completa 484/484, lint y `spec:validate` en verde. Decisión DC-88
